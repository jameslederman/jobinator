---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/jobinator/__init__.py
  - src/jobinator/db.py
  - src/jobinator/models/__init__.py
  - src/jobinator/models/job.py
  - src/jobinator/models/budget.py
  - src/jobinator/agents/__init__.py
  - src/jobinator/tools/__init__.py
  - src/jobinator/scoring/__init__.py
  - src/jobinator/memory/__init__.py
  - src/jobinator/configs/__init__.py
  - src/jobinator/configs/settings.py
  - src/jobinator/cli.py
  - alembic.ini
  - alembic/env.py
  - alembic/script.py.mako
  - .env.example
  - .pre-commit-config.yaml
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_models.py
autonomous: true
requirements:
  - INFR-04
  - DISC-06

must_haves:
  truths:
    - "SQLite database initializes on first run with all required tables via Alembic"
    - "NormalizedJob model has all schema fields including salary quad, location, freshness metadata"
    - "StatusEvent model supports event-sourced status tracking with append-only pattern"
    - "Settings load from TOML config and .env files with typed validation"
  artifacts:
    - path: "src/jobinator/models/job.py"
      provides: "NormalizedJob, StatusEvent, RawJob SQLModel tables"
      contains: "class NormalizedJob"
    - path: "src/jobinator/models/budget.py"
      provides: "SpendRecord, DecisionLog SQLModel tables"
      contains: "class SpendRecord"
    - path: "src/jobinator/db.py"
      provides: "SQLite engine creation with WAL mode, session factory"
      exports: ["get_engine", "get_session"]
    - path: "src/jobinator/configs/settings.py"
      provides: "Settings class loading TOML + .env"
      contains: "class Settings"
    - path: "alembic/env.py"
      provides: "Alembic migration config targeting SQLModel.metadata"
      contains: "target_metadata = SQLModel.metadata"
  key_links:
    - from: "alembic/env.py"
      to: "src/jobinator/models/"
      via: "imports all model modules to register tables"
      pattern: "from jobinator.models"
    - from: "src/jobinator/configs/settings.py"
      to: "~/.config/jobinator/config.toml"
      via: "pydantic-settings TOML source"
      pattern: "toml"
---

<objective>
Scaffold the Jobinator project from scratch and build the complete data layer.

Purpose: Every downstream component (normalization, filtering, budget tracking, output management) needs typed models to write into, a database to persist to, and settings to configure against. This plan creates the entire foundation.

Output: A working Python project with uv, all SQLModel table definitions, Alembic migrations, settings configuration, and passing model tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project scaffold with uv, dependencies, and source layout</name>
  <files>pyproject.toml, src/jobinator/__init__.py, src/jobinator/cli.py, src/jobinator/agents/__init__.py, src/jobinator/tools/__init__.py, src/jobinator/scoring/__init__.py, src/jobinator/memory/__init__.py, .env.example, .pre-commit-config.yaml, tests/__init__.py</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (dependency versions, project structure)
    - .planning/phases/01-foundation/01-CONTEXT.md (decisions D-13 through D-17)
  </read_first>
  <action>
1. Check if uv is installed (`which uv`). If not, install it: `curl -LsSf https://astral.sh/uv/install.sh | sh` and reload PATH.

2. **Install Python 3.12 via uv** (system Python may be 3.9.6 which lacks tomllib needed by pydantic-settings):
   ```
   uv python install 3.12
   ```
   This ensures a 3.12 interpreter is available regardless of system Python version.

3. Initialize the project in the repo root (per D-13), pinning to Python 3.12:
   ```
   uv init --lib --name jobinator --python 3.12
   ```
   If uv init fails because pyproject.toml already exists or directory is non-empty, manually create pyproject.toml instead.

4. Ensure pyproject.toml has:
   - `[project]` with name="jobinator", requires-python=">=3.12"
   - `[build-system]` with hatchling
   - `[tool.ruff]` with line-length=100, target-version="py312"

5. Add core runtime dependencies (per research versions):
   ```
   uv add "sqlmodel>=0.0.21" "alembic>=1.13" "pydantic>=2.7" "pydantic-settings>=2.3"
   uv add "typer>=0.12" "rich>=13.7" "rapidfuzz>=3.9" "platformdirs>=4.2"
   uv add "python-dotenv>=1.0"
   ```

6. Add dev dependencies:
   ```
   uv add --dev "pytest>=8.2" "factory-boy>=3.3" "ruff>=0.5" "mypy>=1.10" "pre-commit>=3.7"
   ```

7. Create source layout (per D-16 — ALL subpackages):
   ```
   src/jobinator/__init__.py          — version = "0.1.0"
   src/jobinator/cli.py               — minimal Typer app with `app = typer.Typer(name="jobinator")`
   src/jobinator/models/__init__.py   — empty, will import from submodules
   src/jobinator/models/job.py        — placeholder (Task 2 fills this)
   src/jobinator/models/budget.py     — placeholder (Task 2 fills this)
   src/jobinator/agents/__init__.py   — empty placeholder (per D-16)
   src/jobinator/tools/__init__.py    — empty placeholder (per D-16)
   src/jobinator/pipelines/__init__.py
   src/jobinator/scoring/__init__.py  — empty placeholder (per D-16)
   src/jobinator/memory/__init__.py   — empty placeholder (per D-16)
   src/jobinator/configs/__init__.py
   src/jobinator/budget/__init__.py
   src/jobinator/output/__init__.py
   tests/__init__.py
   ```

8. Create `.env.example` with:
   ```
   # LLM Provider API Keys
   ANTHROPIC_API_KEY=
   OPENAI_API_KEY=

   # Database (optional — defaults to ~/.local/share/jobinator/jobinator.db)
   # DATABASE_URL=sqlite:///path/to/custom.db
   ```

9. Create `.pre-commit-config.yaml` (per CLAUDE.md requirement for pre-commit >=3.7):
   ```yaml
   repos:
     - repo: https://github.com/astral-sh/ruff-pre-commit
       rev: v0.5.0
       hooks:
         - id: ruff
           args: [--fix]
         - id: ruff-format
     - repo: https://github.com/pre-commit/mirrors-mypy
       rev: v1.10.0
       hooks:
         - id: mypy
           additional_dependencies: [sqlmodel, pydantic, pydantic-settings, typer]
           args: [--ignore-missing-imports]
   ```
   Then install the hooks: `uv run pre-commit install`

10. Verify: `uv run python -c "import jobinator; print(jobinator.__version__)"` prints "0.1.0".
  </action>
  <verify>
    <automated>cd /Users/jameslederman/src/jobinator && uv run python -c "import jobinator; print(jobinator.__version__)" && uv run python --version | grep -q "3.12" && ls .git/hooks/pre-commit</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains `name = "jobinator"`
    - pyproject.toml contains `requires-python = ">=3.12"`
    - pyproject.toml contains `sqlmodel` in dependencies
    - pyproject.toml contains `rapidfuzz` in dependencies
    - pyproject.toml contains `pre-commit` in dev dependencies
    - src/jobinator/__init__.py contains `__version__ = "0.1.0"`
    - src/jobinator/cli.py contains `app = typer.Typer`
    - .env.example contains `ANTHROPIC_API_KEY`
    - .pre-commit-config.yaml exists with ruff and mypy hooks
    - .git/hooks/pre-commit exists (pre-commit hooks installed)
    - `uv run python --version` reports Python 3.12.x
    - `uv run python -c "import jobinator"` exits 0
    - directories exist: src/jobinator/models/, src/jobinator/pipelines/, src/jobinator/configs/, src/jobinator/budget/, src/jobinator/output/, src/jobinator/agents/, src/jobinator/tools/, src/jobinator/scoring/, src/jobinator/memory/, tests/
  </acceptance_criteria>
  <done>uv project initialized with Python 3.12, all dependencies installed, source layout created with ALL D-16 subpackages, pre-commit hooks installed and verified, import succeeds</done>
</task>

<task type="auto">
  <name>Task 2: SQLModel tables, settings, DB engine, Alembic migrations, and tests</name>
  <files>src/jobinator/models/job.py, src/jobinator/models/budget.py, src/jobinator/models/__init__.py, src/jobinator/db.py, src/jobinator/configs/settings.py, src/jobinator/configs/__init__.py, alembic.ini, alembic/env.py, alembic/script.py.mako, tests/conftest.py, tests/test_models.py</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 1: SQLModel tables, Pattern 5: Alembic integration)
    - .planning/phases/01-foundation/01-CONTEXT.md (decisions D-01 through D-05, D-14, D-17)
    - src/jobinator/__init__.py (verify project structure from Task 1)
    - pyproject.toml (verify deps installed)
  </read_first>
  <action>
1. Create `src/jobinator/models/job.py` with these exact SQLModel table classes:

   **Enums** (all str, Enum):
   - `JobStatus` with values: discovered, scored, applied, phone_screen, interview, rejected, offer (per D-03)
   - `LocationType` with values: remote, hybrid, onsite, unknown (per D-02)
   - `SalarySource` with values: posted, estimated, unknown (per D-01)

   **NormalizedJob(SQLModel, table=True)** with fields:
   - `id: str = Field(primary_key=True)` — uuid4 as string
   - `source: str` — source adapter name (greenhouse, lever, hn, wellfound)
   - `source_url: str = Field(unique=True, index=True)` — original posting URL
   - `title: str` — original title text
   - `title_normalized: str = Field(index=True)` — lowercased, normalized title (per D-05)
   - `company: str` — original company name
   - `company_slug: str = Field(index=True)` — deterministic slug (per D-04)
   - `location_raw: Optional[str] = None` — free text from posting (per D-02)
   - `location_type: Optional[str] = None` — LocationType enum value as str (per D-02)
   - `salary_min: Optional[int] = None` — posted salary min (per D-01)
   - `salary_max: Optional[int] = None` — posted salary max (per D-01)
   - `estimated_salary_min: Optional[int] = None` — estimated salary min (per D-01)
   - `estimated_salary_max: Optional[int] = None` — estimated salary max (per D-01)
   - `salary_source: Optional[str] = None` — SalarySource enum value as str (per D-01)
   - `description: str` — full job description text
   - `requirements_raw: Optional[str] = None` — extracted requirements section if available
   - `description_hash: str = Field(index=True)` — sha256 first 500 chars, truncated to 16 hex chars (per D-05)
   - `posted_at: Optional[datetime] = None` — when job was posted (per DISC-06)
   - `first_seen_at: datetime = Field(default_factory=datetime.utcnow)` — first discovery (per DISC-06)
   - `last_seen_at: datetime = Field(default_factory=datetime.utcnow)` — last sighting (per DISC-06)
   - `raw_json: str` — JSON string of original payload for debugging

   **StatusEvent(SQLModel, table=True)** with fields (per D-03):
   - `id: str = Field(primary_key=True)` — uuid4
   - `job_id: str = Field(foreign_key="normalizedjob.id", index=True)`
   - `status: str` — JobStatus enum value as str
   - `reason: Optional[str] = None` — why status changed
   - `created_at: datetime = Field(default_factory=datetime.utcnow)`

2. Create `src/jobinator/models/budget.py` with:

   **SpendRecord(SQLModel, table=True)** with fields:
   - `id: str = Field(primary_key=True)` — uuid4
   - `job_id: Optional[str] = Field(default=None, foreign_key="normalizedjob.id", index=True)`
   - `model_name: str` — LLM model used (e.g. "claude-3-haiku")
   - `provider: str` — provider name (anthropic, openai, etc)
   - `operation: str` — what the call was for (score, generate_resume, etc)
   - `input_tokens: int`
   - `output_tokens: int`
   - `cost_usd: float`
   - `recorded_at: datetime = Field(default_factory=datetime.utcnow)`

   **DecisionLog(SQLModel, table=True)** with fields (per INFR-06):
   - `id: str = Field(primary_key=True)` — uuid4
   - `job_id: Optional[str] = Field(default=None, foreign_key="normalizedjob.id")`
   - `decision_type: str` — e.g. "filter_reject", "score_skip", "budget_exceeded", "apply_approve"
   - `decision: str` — what was decided
   - `reason: str` — why
   - `context_json: Optional[str] = None` — additional context as JSON string
   - `created_at: datetime = Field(default_factory=datetime.utcnow)`

3. Update `src/jobinator/models/__init__.py` to import and re-export all models:
   ```python
   from jobinator.models.job import NormalizedJob, StatusEvent, JobStatus, LocationType, SalarySource
   from jobinator.models.budget import SpendRecord, DecisionLog

   __all__ = [
       "NormalizedJob", "StatusEvent", "JobStatus", "LocationType", "SalarySource",
       "SpendRecord", "DecisionLog",
   ]
   ```

4. Create `src/jobinator/configs/settings.py`:
   - Import from `pydantic_settings import BaseSettings`
   - Import `platformdirs` for XDG paths
   - Class `Settings(BaseSettings)` with:
     - `database_url: str` — default `f"sqlite:///{platformdirs.user_data_dir('jobinator')}/jobinator.db"`
     - `config_dir: str` — default `platformdirs.user_config_dir("jobinator")`
     - `output_dir: str` — default `"~/jobinator-output"` (per D-10)
     - `anthropic_api_key: str = ""`
     - `openai_api_key: str = ""`
     - `daily_budget_usd: float = 5.00`
     - `per_job_budget_usd: float = 0.50`
     - `budget_warn_threshold: float = 0.80`
     - `fuzzy_match_threshold: int = 90` (per D-04)
   - Configure `model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8", toml_file=...)` to load from TOML at `{config_dir}/config.toml` (per D-14)
   - Add `def get_settings() -> Settings:` factory function with `@lru_cache`
   - Update `src/jobinator/configs/__init__.py` to export `Settings, get_settings`

5. Create `src/jobinator/db.py`:
   ```python
   from sqlalchemy import event
   from sqlmodel import SQLModel, Session, create_engine
   from jobinator.configs.settings import get_settings

   def get_engine(url: str | None = None):
       db_url = url or get_settings().database_url
       engine = create_engine(db_url, echo=False)
       # Enable WAL mode for SQLite
       @event.listens_for(engine, "connect")
       def set_sqlite_pragma(dbapi_connection, connection_record):
           cursor = dbapi_connection.cursor()
           cursor.execute("PRAGMA journal_mode=WAL")
           cursor.close()
       return engine

   def get_session(engine=None) -> Session:
       if engine is None:
           engine = get_engine()
       return Session(engine)

   def init_db(engine=None):
       if engine is None:
           engine = get_engine()
       SQLModel.metadata.create_all(engine)
   ```

6. Initialize Alembic:
   ```
   cd /Users/jameslederman/src/jobinator && uv run alembic init alembic
   ```
   Then edit `alembic/env.py`:
   - Add `import sys, os; sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "src"))`
   - Add `from sqlmodel import SQLModel`
   - Add `from jobinator.models import job, budget  # noqa: F401 — register all tables`
   - Set `target_metadata = SQLModel.metadata`
   - In `run_migrations_online()`, use `from jobinator.configs.settings import get_settings; url = get_settings().database_url`
   
   Edit `alembic.ini`:
   - Set `sqlalchemy.url` to empty (will be set programmatically from settings)
   - Set `script_location = alembic`

7. Generate initial migration:
   ```
   cd /Users/jameslederman/src/jobinator && uv run alembic revision --autogenerate -m "initial schema"
   ```

8. Create `tests/conftest.py`:
   ```python
   import pytest
   from sqlmodel import SQLModel, Session, create_engine
   from jobinator.models import NormalizedJob, StatusEvent, SpendRecord, DecisionLog

   @pytest.fixture
   def engine():
       engine = create_engine("sqlite:///:memory:")
       SQLModel.metadata.create_all(engine)
       return engine

   @pytest.fixture
   def session(engine):
       with Session(engine) as session:
           yield session
   ```

9. Create `tests/test_models.py` with tests:
   - `test_create_normalized_job` — insert a NormalizedJob with all required fields including salary quad (D-01), location fields (D-02), freshness timestamps (DISC-06), description_hash. Query it back, assert all fields match.
   - `test_create_status_event` — insert NormalizedJob, then append two StatusEvents (discovered, then scored). Query events for the job ordered by created_at, assert 2 events, latest is "scored" (D-03).
   - `test_create_spend_record` — insert SpendRecord with model_name, cost_usd, token counts. Query back, verify cost_usd matches.
   - `test_create_decision_log` — insert DecisionLog with decision_type="filter_reject". Query back, verify fields (INFR-06).
   - `test_job_unique_source_url` — insert two jobs with same source_url, expect IntegrityError.
   - `test_freshness_metadata` — create job, verify first_seen_at and last_seen_at are populated (DISC-06).
   - `test_alembic_upgrade_head` — **tests INFR-04 Alembic migration path.** Create a temp SQLite file DB (using `tmp_path` fixture), set `DATABASE_URL` env var to point at it, run `alembic upgrade head` via `subprocess.run(["uv", "run", "alembic", "upgrade", "head"], check=True, env=...)`. Then connect to the temp DB with SQLAlchemy `inspect(engine).get_table_names()` and assert all 4 tables exist: `normalizedjob`, `statusevent`, `spendrecord`, `decisionlog`. This verifies the full Alembic migration path works end-to-end, not just SQLModel.metadata.create_all.
  </action>
  <verify>
    <automated>cd /Users/jameslederman/src/jobinator && uv run pytest tests/test_models.py -v</automated>
  </verify>
  <acceptance_criteria>
    - src/jobinator/models/job.py contains `class NormalizedJob(SQLModel, table=True)`
    - src/jobinator/models/job.py contains `class StatusEvent(SQLModel, table=True)`
    - src/jobinator/models/job.py contains `salary_min: Optional[int]` and `salary_max: Optional[int]` and `estimated_salary_min: Optional[int]` and `estimated_salary_max: Optional[int]`
    - src/jobinator/models/job.py contains `class SalarySource(str, Enum)`
    - src/jobinator/models/job.py contains `class LocationType(str, Enum)`
    - src/jobinator/models/job.py contains `first_seen_at: datetime` and `last_seen_at: datetime`
    - src/jobinator/models/job.py contains `description_hash: str`
    - src/jobinator/models/budget.py contains `class SpendRecord(SQLModel, table=True)`
    - src/jobinator/models/budget.py contains `class DecisionLog(SQLModel, table=True)`
    - src/jobinator/db.py contains `PRAGMA journal_mode=WAL`
    - src/jobinator/configs/settings.py contains `class Settings(BaseSettings)`
    - alembic/env.py contains `target_metadata = SQLModel.metadata`
    - alembic/versions/ contains at least one migration file
    - `uv run pytest tests/test_models.py` exits 0 with all 7 tests passing (including test_alembic_upgrade_head)
  </acceptance_criteria>
  <done>All SQLModel tables defined with correct fields per decisions D-01 through D-05, Alembic configured and initial migration generated, Alembic upgrade path tested end-to-end, settings load from TOML + .env, all model tests pass</done>
</task>

</tasks>

<verification>
1. `uv run python -c "import jobinator; print(jobinator.__version__)"` outputs "0.1.0"
2. `uv run python --version` reports Python 3.12.x
3. `uv run pytest tests/test_models.py -v` — all 7 tests pass (including Alembic upgrade test)
4. `uv run alembic upgrade head` on a fresh DB creates all tables
5. `uv run python -c "from jobinator.models import NormalizedJob, StatusEvent, SpendRecord, DecisionLog; print('OK')"` outputs "OK"
6. `uv run python -c "from jobinator.configs.settings import Settings; print(Settings.__fields__.keys())"` lists all config fields
7. `ls .git/hooks/pre-commit` confirms pre-commit hooks installed
</verification>

<success_criteria>
- Project scaffold complete with all dependencies installed via uv on Python 3.12
- All D-16 subpackages exist: agents/, tools/, pipelines/, scoring/, memory/, configs/
- Pre-commit hooks configured with ruff and mypy and installed to .git/hooks
- All 4 SQLModel table classes exist with correct fields matching decisions D-01 through D-05
- Alembic configured with initial migration
- Alembic upgrade path tested (test_alembic_upgrade_head passes)
- Settings class loads TOML + .env
- DB engine creates SQLite with WAL mode
- All model tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
